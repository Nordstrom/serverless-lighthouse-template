{
  "name": "serverless-lighthouse",
  "version": "1.0.0",
  "description": "Run lighthouse audits from headless chrome in lambda",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jacob Aaron Wrolson",
  "license": "ISC",
  "dependencies": {
    "lighthouse": "^2.7.0",
    "serverless-plugin-chrome": "^1.0.0-31"
  },
  "devDependencies": {
    "serverless-attach-managed-policy": "0.0.6"
  }
}
